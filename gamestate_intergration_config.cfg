{
    // The URI to send the game state data to. This should be the local IP of your Raspberry Pi.
    "uri" "http://192.168.1.100:3000"

    // Define the parts of the game state that you want to receive.
    // We need player and allplayers to get the Steam IDs.
    "data"
        {
            "player" "1"
            "allplayers" "1"
            "map" "1"
            "round" "1"
        }
}